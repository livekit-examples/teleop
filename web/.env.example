NEXT_PUBLIC_ROBOT_IDENTITY=
NEXT_PUBLIC_LIVEKIT_URL=
NEXT_PUBLIC_LIVEKIT_TOKEN=
# Set to "false" to skip the acquire_control RPC handshake; the controller
# then publishes control commands without acquiring the operator seat.
NEXT_PUBLIC_ACQUIRE_CONTROL_ENABLED=true